# wt-rotate — dépendances du serveur de télécommande.
#
# Installé globalement par start_remote.bat (pas de venv) : on fixe des
# minimums sûrs (>=) plutôt que des versions exactes, pour garantir que les
# API utilisées existent sans bousculer les autres paquets Python de la machine.

aiohttp>=3.9          # serveur HTTP + WebSocket (obligatoire)
qrcode>=7.0           # QR code de la télécommande (sinon : QR « manquant »)
cryptography>=3.4     # génération des clés VAPID (notifications push)
pywebpush>=1.14       # envoi des notifications Web Push

# Volume système Windows — optionnel, et uniquement sur Windows (le marqueur
# évite une tentative d'install vouée à l'échec sous Linux/macOS).
pycaw>=20230407; platform_system == "Windows"
